"""Reusable Flet controls used across multiple pages.

Keeping these here (instead of re-declaring them in every page) is what lets a
page stay short and focused on its own layout/state instead of re-inlining the
same dropdown, the same confirm dialog, and the same SQL.
"""
